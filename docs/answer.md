## 业务口径签署确认

### 1) 风险识别与响应（目标：触发正确率≥95%，误触发≤5%，单组合P95时延≤800ms）

**业务确认：✅ 基本通过，需调整部分阈值**

**当前实现评审：**
- ✅ 触发条件合理：波动率阈值+重大事件组合
- ✅ 事件定义完整：熔断、极端相关性崩溃、涨跌停比例>30%、重大市场事件
- ⚠️ 需要调整的阈值：

**分市场差异化阈值调整：**

| 市场 | 波动率阈值基准 | 涨跌停比例阈值 | 重大事件敏感度 |
|------|---------------|---------------|----------------|
| **CN** | 5% (0.05) | 30% → **25%** | 中等（A股事件频发） |
| **US** | 3% (0.03) | 30% → **20%** | 高（美股流动性敏感） |
| **HK** | 4% (0.04) | 30% → **25%** | 高（国际市场联动） |
| **JP** | 4% (0.04) | 30% → **25%** | 中等（日股相对稳定） |
| **EU** | 3.5% (0.035) | 30% → **20%** | 高（欧元区敏感） |
| **SG** | 5% (0.05) | 30% → **30%** | 中等（新兴市场容忍度较高） |

**理由：**
- 美股和欧元区市场对流动性更敏感，阈值应更严格
- A股涨跌停制度下，25%已属极端情况
- 新加坡作为新兴市场，可保持较高容忍度

**时延保障：** 当前实现满足≤800ms目标，但建议增加**异步触发机制**避免阻塞主流程。

### 2) 审计与追溯（目标：report_snapshot完整性=100%；model_health分级准确率≥95%）

**业务确认：✅ 通过，建议增强字段**

**当前字段评审：**
```
# report_snapshot 增强建议
新增字段：
- 'calculation_id': str(uuid.uuid4())  # 唯一计算标识
- 'trigger_reason': 'VOLATILITY_SPIKE'|'MARKET_EVENT'|'SCHEDULED'  # 触发原因
- 'cache_status': 'HIT'|'MISS'|'INVALIDATED'  # 缓存状态
- 'data_freshness_seconds': int  # 数据新鲜度(秒)

# model_health 分级调整
分市场差异化阈值：
CN: {'min_points': 30, 'optimal_points': 180} → ✅ 合理
US: {'min_points': 63, 'optimal_points': 252} → ✅ 合理  
JP: {'min_points': 50, 'optimal_points': 200} → ⚠️ 建议调整为{55, 220}
```

**降级策略业务口径：**
```python
# 各市场降级策略差异化
降级映射 = {
    'CN': {  # A股市场容忍度较高
        'POOR': 'SIMPLIFIED_COVARIANCE',
        'INSUFFICIENT': 'HISTORICAL_SIMULATION'  
    },
    'US': {  # 美股要求严格，尽早降级
        'FAIR': 'SIMPLIFIED_COVARIANCE', 
        'POOR': 'BASIC_STATISTICS'
    }
}
```

### 3) 触发可靠性与成本（目标：平稳市况缓存失效率≤10%；极端市况命中率≥85%）

**业务确认：⚠️ 部分通过，需优化分层策略**

**分层阈值优化建议：**
```python
# 波动率分层优化
VOLATILITY_TIERS = {
    'NORMAL': {'max': 0.05, 'cache_ttl': 3600},      # 1小时
    'MEDIUM': {'max': 0.075, 'cache_ttl': 1800},      # 30分钟  
    'HIGH': {'max': 0.10, 'cache_ttl': 600},          # 10分钟
    'EXTREME': {'max': float('inf'), 'cache_ttl': 60} # 1分钟
}

# 事件权重优化（分市场）
EVENT_WEIGHTS = {
    'US': {  # 美股事件权重
        'circuit_breaker': 0.4,
        'extreme_correlation': 0.3, 
        'limit_hits': 0.2,
        'major_event': 0.5
    },
    'CN': {  # A股事件权重
        'circuit_breaker': 0.6,  # 熔断影响更大
        'extreme_correlation': 0.2,
        'limit_hits': 0.4,       # 涨跌停常见但影响有限
        'major_event': 0.3
    }
}
```

**成本控制保障：**
- ✅ 当前实现支持20%+CPU节省目标
- ⚠️ 建议增加**增量重算**：仅失效受影响组合的缓存
- ⚠️ 建议增加**批量合并**：多个触发事件在短时间窗口内合并处理

### 4) 市场差异化合规（目标：各市场测试通过率=100%）

**业务确认：✅ 通过，需补充特定市场例外**

**各市场最终合规阈值：**

| 市场 | 波动率阈值 | 最小数据点 | 货币严格模式 | 特殊例外 |
|------|-----------|------------|-------------|----------|
| **US** | 3% | 63 | 严格 | 熔断后15分钟内禁止缓存 |
| **CN** | 5% | 30 | 非严格 | 涨跌停日数据质量降级 |
| **HK** | 4% | 50 | 严格 | 港股通标的额外检查 |
| **JP** | 4% | 55 | 严格 | 春/秋分特殊交易日 |
| **EU** | 3.5% | 60 | 非严格 | 夏令时调整窗口 |
| **SG** | 5% | 50 | 严格 | 无特殊例外 |

**例外说明：**
- **US熔断例外**：熔断属于极端事件，需要更长时间的数据稳定期
- **CN涨跌停例外**：A股涨跌停制度导致价格失真，需要特殊处理
- **JP季节例外**：日本市场在特定假日有特殊交易规则

## 代码实现业务评审要点

### 智能缓存失效触发机制 ✅
```python
# 当前实现满足业务需求，建议增加以下优化：
context = {
    'time_window': str(int(time.time())),  # ✅ 满足审计需求
    'param_version': str(config.get('param_version', 'v1')),  # ✅ 版本追溯
    'market_data_updated': bool(data.get('market_data_updated', False)),  # ✅ 数据新鲜度
    'volatility': returns_std,  # ✅ 核心指标
    # 建议新增：
    'trigger_score': calculate_trigger_score(volatility, events),  # 触发分数量化
    'affected_symbols_count': len(affected_symbols)  # 影响范围
}
```

### 报告结构完整性 ✅  
```python
report_snapshot = {
    'report_id': str(uuid.uuid4()),  # ✅ 必要：唯一标识
    'environment': os.getenv('DEPLOYMENT_ENV', 'dev'),  # ✅ 必要：环境区分
    'timestamp': datetime.utcnow().isoformat() + 'Z',  # ✅ 必要：时间戳
    # 当前字段完整，建议保持
}
```

## 最终签署结论

**总体评估：✅ 业务口径基本符合，可进入P2实施**

**调整范围汇总：**
1. **阈值优化**：按市场差异化调整波动率和事件阈值
2. **字段增强**：增加触发原因、缓存状态等审计字段  
3. **分层策略**：优化缓存TTL分层和事件权重
4. **例外处理**：补充各市场特殊情况的处理规则

**实施建议：**
- 优先实施US/CN市场的差异化配置
- 分阶段上线，先验证核心触发逻辑
- 建立实时监控看板，跟踪各项指标达成情况

**风险提示：**
- 极端市场条件下可能超出预设的5%误触发率
- 需要建立快速人工干预机制作为后备

确认后即可冻结参数进入P2实施阶段。